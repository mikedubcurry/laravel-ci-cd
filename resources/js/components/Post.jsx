import React from 'react';
import ReactDOM from 'react-dom';

function Post() {
    return (
        <div>
            
        </div>
    );
}

export default Example;

if (document.getElementById('post')) {
    ReactDOM.render(<Example />, document.getElementById('post'));
}
